--lis update

CREATE TABLE `deliveryreceipt` (
  `id` int(11) NOT NULL AUTO_INCREMENT,
  `no_jo` varchar(45) NOT NULL,
  `penerima` varchar(45) NOT NULL,
  PRIMARY KEY (`id`),
  UNIQUE KEY `id_UNIQUE` (`id`),
  KEY `idjo_idx` (`no_jo`)
);

CREATE TABLE `t_bl` (
  `id` int(11) NOT NULL AUTO_INCREMENT,
  `no_jo` varchar(45) NOT NULL,
  `idusers` int(11) NOT NULL,
  `foto` varchar(45) DEFAULT NULL,
   `nomor_kontainer` varchar(45) DEFAULT NULL,
  PRIMARY KEY (`id`)
) ;

laporan;;;;;

bisa bentuk grafik

jumlah order terbanyak terhadap customer

tujuan order terbanyak terhadap kota tujuan/daerah

by operator

by agen

top 10 komoditas

update controller bl
edit form bl
edit kontainerjo
edit detailcontroller

---20-06-17---
models order
controller JO, kontainerjo, bl
views index, create, _form >> _frm
tabel order penerima set null
views table Containerjo add group by nomor_kontainer
views table Tdetail add group by date